/bin/
/dist/
*.test
*.out
.DS_Store

# auto-fetched libdoltlite cache (see scripts/fetch-doltlite.sh)
/.doltlite/

# autosk's own task DB (don't commit during dev)
/.autosk/

# Local scratch
/tmp/
/scratch/

# pi harness state (local agent scratch)
/.pi/
