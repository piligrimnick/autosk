/bin/
/dist/
*.test
*.out
.DS_Store
# stray Go binaries from `go build ./cmd/autosk` (canonical output is bin/)
/autosk
/autoskd

# Bun daemon workspace build output / installed deps.
/daemon/node_modules/
/daemon/**/node_modules/

# autosk's own task dir (don't commit during dev)
/.autosk/

# Local scratch
/tmp/
/scratch/

# pi harness state (local agent scratch)
/.pi/
