/bin/
/dist/
*.test
*.out
.DS_Store
# stray Go binaries from `go build ./cmd/autosk` (canonical output is bin/)
/autosk
/autoskd

# Rust (autosk-core / autoskd / autosk-proto workspace) build output.
# Cargo.lock IS committed (the workspace ships binaries).
/target/

# auto-fetched libdoltlite cache (see scripts/fetch-doltlite.sh)
/.doltlite/

# autosk's own task DB (don't commit during dev)
/.autosk/

# Local scratch
/tmp/
/scratch/

# pi harness state (local agent scratch)
/.pi/
