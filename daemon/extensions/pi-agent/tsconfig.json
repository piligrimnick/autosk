{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts", "test/**/*.ts"],
  "comment": "pi-transit-extension.ts and pi-mcp-extension.ts are loaded by pi's toolchain (imports pi's typebox / ExtensionAPI), never by the autosk daemon, which only passes their path to `pi -e`. Excluded so the workspace typecheck does not need pi's deps.",
  "exclude": ["src/pi-transit-extension.ts", "src/pi-mcp-extension.ts"]
}
