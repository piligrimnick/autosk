//! `autosk-proto` — JSON-RPC wire contract for autoskd (plan §5).
//!
//! Phase 0 stub. The serde mirrors of the `api.*` / `datasource.*` structs and
//! the JSON-RPC method/notification enums are introduced in Phase 1; this crate
//! exists now only so the Cargo workspace skeleton matches the target topology
//! described in `docs/plans/20260607-Rust-Daemon-Tauri-GUI.md`.
